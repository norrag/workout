# notes — the owner's running field-notes, tracked

This folder is Claude's working notebook for the stream of notes the owner
captures while using WORKOUT (bugs, questions, ideas, UX nits). The owner hands
over raw notes periodically and works the backlog **through Claude** — Claude
owns and maintains the structure here.

- **How this area operates:** [`CLAUDE.md`](./CLAUDE.md) — the intake protocol,
  lifecycle, and consolidation/purge rules. Read it first.
- **Current state of every open item:** [`backlog.md`](./backlog.md).
- **Work that is built but not merged:** [`backlog.md` → Open pull
  requests](./backlog.md#open-pull-requests) — check this at every session start,
  not just the merged-PR sweep.
- **What happened recently:** [`log.md`](./log.md) (newest first).
- **Closed/shipped items:** [`archive.md`](./archive.md).

## Workstreams

Items are grouped so related work ships together; a workstream gets a detail
file once it's actively worked. Re-cut these whenever intake reveals a better
grouping.

| ID | Workstream | Detail file | What it covers |
|----|-----------|-------------|----------------|
| **A** | Engine & metrics Q&A | [`A-engine-metrics.md`](./A-engine-metrics.md) | How e1RM, strength gain, progression, misses, baselining, set-planning, deloads work — answered from code + spec. **All question rows (S1–S8, PH39) answered and archived 2026-08-14**; the detail file stays as the substance. Open: **T-A5** only (graded MEV→MAV→MRV ramp + MRV-stop auto-deload, deferred — and the one claim doc 22 `22b` had to correct). |
| **B** | e1RM audit & exposure | _shipped_ | Store per-set e1RM, expose to MCP, tap-to-flip e1RM view in history. |
| **C** | Stats unification | _tbd_ | Meso ↔ macro stats parity, planned-sets definition, aggregate strength gains. Shipped + **activated**: two-component strength-rate band v23 + the v24 `rate_source:"plan"` flip (N43, PRs #182/#183, live 2026-07-12 and carried into the active v27); BodySpec DEXA (N34, PRs #173–#176). Open: **N52** — the DEXA copy amendment *and* N54's `REALISTIC TARGET` re-enable were both told to ride that activation and **never shipped**; a pure view change, now `ready`. |
| **D** | Macrocycle & meso management | _tbd_ | Custom-duration input bug, unplanned-meso display, meso management under a macro. Open: custom-template editing (**N46**, `ready`, scoped in `scoping.md`). |
| **E** | Logging & feedback UX | _tbd_ | Feedback slider resolution, note icon, back-button clutter, set-type removal, over/under marker, soreness capture. Shipped: replace-sheet filters + confirm step (N48/N49); **background set-logging queue** (N68, now hard rule 9) + thumb-only sliders (N69), PR #215; the **day-view focus pass** (N82, PR #240, released in 1.1.0). Open: offline READS (**T-N68a**, needs an owner call). |
| **F** | Settings, profile & search | _tbd_ | Settings cleanup, template-code flow, profile height units, live search filter, bodyweight-exercise settings. |
| **G** | Bugs | _tbd_ | Discrete defects to reproduce + fix. Shipped: PH29/PH38/PH35/PH28, R8–R13 (2026-07-01 review, PRs #95–#119), N5, N11, N50, N51, the iOS tab-bar detach (N47, PR #186), the increment lattice off the last entered weight (N67, PR #215), and Batch 32's N76/N78 (PR #226). **Open and blocking: N85** — the migration-drift guard + `skipWorkout` + week-boundary gate, built as PR #222 and **never merged**; and **N79**, whose migration was never applied to hosted so concurrent mesos are dark. |
| **H** | Prescription explanation & owner decisions | [`../19-prescription-explanation-v3.md`](../19-prescription-explanation-v3.md) | Originally the needs-a-product-decision pile (**all decided 2026-07-02**, Batch 4: PH33 → ready, P21 → verify, PH30 → deferred, which then became doc 18 → doc 19). Now the explanation layer: **N60** in progress — phases 1–3 shipped (PRs #201/#202), phase 3 awaits the owner's voice-read, phases 4–5 owner-gated per doc 19 §11 — plus the five spin-offs **T-N60b–f** (structured pain follow-up, note classification, equipment/setup identity, deviation reasons, review synthesis), all `ready` and none started. |
| **I** | Engine v9 cleanup | [`I-engine-v9.md`](./I-engine-v9.md) _(shipped)_ | Retire the legacy increment path; fold bodyweight / cold-start / big-miss into a clean v9 model. Complete (T-I1–T-I5, engine_params v16 active); detail file kept for history. |
| **J** | Performance & efficiency | [`J-performance.md`](./J-performance.md) | Speed/efficiency pass (**N1**, in progress): measure-first baseline, then client bundle/render wins + query-scope/caching. Heavy lifting already lives in the backend; the engine stays pure TS (not relocated to edge/DB). Shipped since: per-route skeletons (PR #134), the launch splash (N53, PR #187), the queue echo rule (N73, PR #220). |
| **K** | Integrity & security hardening | [`../reviews/2026-07-01-repo-review.md`](../reviews/2026-07-01-repo-review.md) §1/§4 | From the 2026-07-01 repo review: shares exploit (R1), dead CI/RLS guardrail (R2), non-atomic plan/param writes + missing uniques (R3), history-deleting regeneration (R4), completion-lock bypasses (R5), UTC dates (R6), SW auth-cache purge (R7), MCP polish (R25). |
| **L** | Delivery guardrails & observability | [`../reviews/2026-07-01-repo-review.md`](../reviews/2026-07-01-repo-review.md) §5 | From the same review: production error observability (R20), e2e/integration/golden-v16 coverage (R21), env fail-fast (R22), repo hygiene (R23). |
| **M** | In-app help & education | [`../22-user-manual.md`](../22-user-manual.md) | The term-level layer (N25 `InfoDot`, N81 `InlineTerm`) and the mechanism-level layer above it (**N74** — the 21-chapter User Guide incl. ch. 18 *Training with AI*, connector retrieval, and the `22e` link-placement grammar). **Shipped and released in 1.1.0** (PRs #231–#246, #247). Open: **N74 Phase 4** (owner cold read + re-validate every `22a` claims-ledger row against code) and **Phase 8** (the maintenance rule that keeps the manual from rotting). |
| **Q** | MEASURE companion app | [`../20-measure-companion-app.md`](../20-measure-companion-app.md) | The measurement front end (N66): bodyweight series + smoothing/reports, fast capture (token API + Shortcuts + the Apple Health bus), circumferences, DEXA relocation, import/export, the three-source synthesis, and the WORKOUT seam. Direction fixed through owner review round 1 (2026-07-31); blocked on the doc-20 §17 decisions, then Phase 0's mockup pass (hard rule 8) before any build. |
| **P** | Prescribed progression | [`../16-prescribed-progression.md`](../16-prescribed-progression.md) | Earned-step overload + macro-rate pacing (**N35**, complete and live — phases 1–4 in PRs #158–#161, Phase R prepared in #162, and v20's behavior active since v21 on 2026-07-11). Of the doc-16 §11 deferred spine: **N36** (envelope loop, live in v25) and **N37** (`rate_source:"plan"`, live in v24) are done; **N38** (periodic honest-RIR confirmation — capture shipped in #216, the check still deferred) and **N39** (per-exercise progression-off, deferred on demand) stay open. Exercise-level RIR (**N70**, all six doc-21 phases + the measuring band, armed 2026-08-10 and re-cut in v27 on 2026-08-12) is done. Open: **N72** (bounded substitution + the `LOOKBACK_WEEKS = 2` cliff, deferred). |
| **R** | Notes-area tooling | _tbd_ | Running this area from the MCP connector rather than only from a Claude Code session (**N86**): the manual served verbatim, the index parsed, and intake/status/log writes as atomic commits against `docs/notes/**`. Built as PR #212 and **open since 2026-07-30**; needs `NOTES_REPO_TOKEN`, a rebase, and a parser re-run. |
| **S** | Post-1.1.0 loose ends | _tbd_ | Items surfaced by the manual and release work that belong to no earlier stream: **N83** (bar speed as a proximity-to-failure cue — `ready` now that Phase 7a settled the affordance grammar) and **N84** (the e2e suite is red on `main`, `bodyspec-integration.spec.ts` looking deterministic within a run — blocks a clean base-branch comparison for every PR). |
| **V** | Versioning & releases | [`../23-versioning-releases.md`](../23-versioning-releases.md) | Discrete versioned releases under `MAJOR.FEATURE.FIX` (**N80**, done): the repo-side release registry, `profiles.last_seen_version` + the What's New gate, version history on More, ID-free deep links sharing doc 22's section-ID validator, and the announce-then-activate coupling on `engine_params`. All seven doc-23 phases merged in PR #230; **v1.0.0 cut 2026-08-06, v1.1.0 (the Guide block) cut 2026-08-13** in PR #247. Ongoing rather than open: the release process itself, in [`../deployment/release.md`](../deployment/release.md). |
